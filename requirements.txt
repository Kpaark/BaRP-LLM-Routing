# Core numerical stack
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0

# RouterBench download (barp.setup_data)
huggingface_hub>=0.20.0

# Policy training (Phase 2-3)
torch>=2.2.0

# Splits + reference baselines + Pareto plots
scikit-learn>=1.4.0
matplotlib>=3.7.0

# Per-prompt inference CLI (only required by `python -m barp.infer --prompt ...`)
sentence-transformers>=2.6.0

# Experiment tracking (optional; enabled with --wandb in barp.train)
wandb>=0.16.0

# Notebook (optional, handy for the eval Pareto sweep)
jupyter>=1.0.0
ipykernel>=6.0.0
